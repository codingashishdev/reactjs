import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    
}

const postSlice = createSlice({
    name: "post",
    initialState,
    reducers: {
        
    }
})

export const {} = postSlice.reducer
export default postSlice.actions
